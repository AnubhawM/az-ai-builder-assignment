# Database (SQLite - zero config, auto-created on first run)
DATABASE_URL=

SLIDESPEAK_API_KEY=

# Workflow reliability + PPT generation paths
WORKFLOW_RUN_STALE_TIMEOUT_SECONDS=330

# Output directory for PPT files
PPT_OUTPUT_DIR=

# Slidespeak skill directory (usually in ~/.openclaw/workspace/skills/slidespeak)
SLIDESPEAK_SKILL_DIR=

# Slidespeak script path (usually in ~/.openclaw/workspace/skills/slidespeak/scripts/slidespeak.mjs)
SLIDESPEAK_SCRIPT_PATH=

# SQLite thread/concurrency tuning (used for file-backed SQLite only)
SQLITE_POOL_SIZE=10
SQLITE_MAX_OVERFLOW=20

# Directory for workflow file uploads
WORKFLOW_UPLOADS_DIR=